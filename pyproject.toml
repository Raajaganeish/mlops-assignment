[tool.black]
line-length = 88
target-version = ["py310"]
skip-string-normalization = false

[tool.isort]
profile = "black"
line_length = 88
known_first_party = ["api"]
skip = ["models", "data", "build", "dist", "__pycache__"]

[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]
